[Unit]
Requires=local-fs.target

[Service]
EnvironmentFile=/boot/config/hw.env
ExecStart=/sbin/luksd '${LUKS_BLKDEV}' '${LUKS_NAME}' root locky
Restart=on-failure
User=root

[Install]
WantedBy=multi-user.target
